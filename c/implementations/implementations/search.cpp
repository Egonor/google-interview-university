#include "stdafx.h"
#include "vector.h"

// Binary Searches a sorted Vector
/*
int BinarySearch(Vector v, int value) {
    bool sorted = true;

    if (sorted) {
        int cursor = (v.Capacity() / 2)
        for (int i = 0; i < v.Capacity(); ++i) {
            if ((v.Capacity() / 2) == value);
        }
    }
    else {
        printf("ERROR: Attempted to search an unsorted Vector!");
        return -1;
    }

    v.Capacity();

}


// Recursive implementation
int BinarySearchR(Vector v) {

}
*/